---
title: Заметка передачи работы
type: handoff
status: active
updated: 2026-09-24
tags: [handoff, status]
---

# Передача работы

Правило команды: кто пишет, тот пишет. Закончив порцию, он обновляет эту заметку: что сделано, что дальше, как запустить, что сломано.

## Сейчас (24.09, вечер) — сессия по [GEN_V2_PLAN](GEN_V2_PLAN.md)
- **Порядок частей:**
  - 0 приборка;
  - 1 код-ревью;
  - 2 UX-проход;
  - 3 подтверждение ремонта;
  - 4 телефон для мастера;
  - 5 кабинет района;
  - 6 импорт и геокодер;
  - 7 председатель совета.
- **Готово:** —.
- **Дальше:** часть 1 (код-ревью спайка `af2e939..2ed2593`).

## Сделано до этой сессии
Все спайки ниже в коде и в `docs/`. Детали — в `git log` и ADR.

| Когда | Что | Коммиты |
|---|---|---|
| 24.09 | БД и API: миграции goose, sqlc, репозитории, сценарии, HTTP API v1, вход по initData и демо-роли, webhook с дедупликацией, образ `api` с сертификатами Минцифры | `9ea4be5`…`4edbc56` |
| 25.09 | Мини-приложение (дом, форма в 3 шага с проверкой дубля, карточка, очередь УК со сменой статуса), outbox и живая карточка в боте, диалог бота | `670f075`…`af2e939` |
| 24–25.09 | DI-контейнер в `cmd/api`, негативные тесты API, порог покрытия в CI (ADR-010) | `90c8a7f`, `c5e8ee5` |
| 24.09 | Просрочка: отметка `overdue_at`, уведомления, строка в хронологии | `fef7799` |
| 24.09 | Метрики УК и синтетический пример данных со сдвигом дат | `3cfa0dd` |
| 24.09 | QR-наклейки для печати | `10c01ea` |
| 24.09 | Удаление аккаунта; демо-пользователь восстанавливается | `5f2dea3` |
| 24.09 | Материалы к сдаче: README, `docs/`, DATA-API.yaml, запуск одной командой (ADR-013) | `1a0aec0` |
| 24.09 | Подсказка категории: правила + Ollama `qwen3:4b` (ADR-014) | `13c4ecb`, `912ff2d` |
| 24.09 | PDF-обращение в Мосжилинспекцию по подписанной ссылке | `c1ea9d2` |
| 24.09 | Фото к заявке на томе без EXIF (ADR-015) | `77479db` |

Состояние качества на `2ed2593`:
- покрытие бэкенда 91,3% (`task test:cover`), Vitest по `src/shared/lib` 99%;
- сборка Docker с нуля 163 с.

## Не сделано (люди)
- **DevOps:**
  - деплой по `docs/deployment.md` (`deploy/.env` с серверными значениями);
  - URL мини-приложения на модерацию (до 48 рабочих часов);
  - живая проверка в MAX: живая карточка, фото из бота, `downloadFile` для PDF, QR с телефона.
- **Перед сдачей:**
  - заменить `<домен-стенда>` в README, `DATA-API.yaml`, `docs/testing.md`, `deploy/seed/test-data.json`;
  - выбрать название продукта (рабочее — «Заявки по дому в MAX»);
  - презентация PDF;
  - экспорт чистого репозитория с проверкой ИИ-меток (ADR-007).
- **LLM на VPS.** Профиль `llm` включать, если на сервере 6+ ГБ памяти (модели нужно около 4 ГБ).

## Как запустить
Всё через `task` (список: `task`), переменные — из `.env` в корне.
```bash
task db                 # Postgres в Docker
task test               # юнит-тесты
task test:integration   # тесты на Postgres
task run                # api на :8080, миграции при старте, BOT_MODE=off
task miniapp:dev        # мини-приложение на :5173, /?demo=resident | resident_2 | uk
```
Как у жюри, одной командой: `docker compose -f deploy/compose.yaml --env-file deploy/.env.example up -d --build`, затем `http://localhost/?demo=resident`.

## Известные проблемы
- **Превью агента.** Если `:8080` занят (зависший `go run`), запускать `api-8081` и `miniapp-8081` из `.claude/launch.json`: прокси Vite берёт адрес из `API_PROXY`.
- **Vite на Windows не видит правки через `sed -i`.** Модуль остаётся старым, поэтому файлы `miniapp/src` правятся только через редактор.
- **Кириллица в `curl` из консоли Windows** уходит в cp1251, и API отвечает 422. Тело нужно брать из файла в UTF-8: `--data-binary @file.json`.
- **Webhook и polling.** После включения webhook на сервере long polling перестаёт работать у всех: локально держать `BOT_MODE=off`.
- **Документация `Update` в `dev-max/`** описывает только `bot_added`. Поля `bot_started` и `message_callback` взяты из `POST /answers`, первые реальные события нужно сверить в логах.
- **Сроки в `rules`** модельные, праздники не учитываются (записано в `docs/data.md`).
- **Git предупреждает про LF → CRLF.** Не мешает.

## Открытые вопросы
- Домен демо-стенда: пришлёт DevOps.
- Название продукта.
