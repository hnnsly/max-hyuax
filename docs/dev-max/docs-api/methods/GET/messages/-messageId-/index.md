# Получение сообщений

> Для корректной работы ваших чат-ботов и мини-приложений направляйте запросы на домен `platform-api2.max.ru` вместо `platform-api.max.ru`. Также убедитесь, что добавили сертификат Минцифры в список доверенных

**GET** `/messages/{messageId}`

Возвращает сообщение из канала или чата по его ID

#### Пример запроса:

```bash
curl -X GET "https://platform-api2.max.ru/messages/{messageId}" \
  -H "Authorization: {access_token}"
```

## Авторизация

- **`access_token`** — apiKey

  > Передача токена через query-параметры больше не поддерживается — используйте заголовок `Authorization: <token>`

  Токен для вызова HTTP-запросов присваивается при создании бота — его можно найти на [платформе](https://business.max.ru/self) в разделе **Чат-боты**. Выберите необходимого бота и нажмите **⋮** → **Настройки** → значок копирования справа от поля с токеном

  Eсли вы верифицировали профиль и создали бота в [мини-приложении «MAX для бизнеса»](https://max.ru/business_bot?startapp), получить токен можно там же или в [боте «MAX для бизнеса»](https://max.ru/business_bot) с помощью команды **Получить токен**

  Рекомендуем не разглашать токен посторонним, чтобы они не получили доступ к управлению ботом.
  Токен может быть отозван за нарушение Правил платформы

## Параметры

- **`messageId`** — string [a-zA-Z0-9_\-]+ [a-zA-Z0-9_\-]+

  ID сообщения (`mid`), чтобы получить одно сообщение в чате или канале

## Результат

- **`sender`** — object [User](../../../../objects/User.md) · optional

  Отправитель сообщения: пользователь или бот

  **Обратите внимание**:  
  - Все параметры в объекте относятся к отправителю сообщения  
  - Объект отсутствует, если сообщение отправлено в канал

- **`recipient`** — object Recipient

  Получатель сообщения: пользователь или бот (для диалога), канал или чат

  **Обратите внимание**: все параметры в объекте относятся к получателю сообщения

  - **`chat_id`** — integer &lt;int64&gt; · Nullable

    ID чата или канала. Как получить ID — в [разделе «Получение chat_id»](../../../../index.md#Получение%20chat_id)

  - **`chat_type`** — enum ChatType

    Возможные значения в enum: `"chat"` `"channel"` `"dialog"`

    Тип чата:
    - `chat` — групповой чат
    - `channel` — канал или комментарий к посту (для вызовов методов группы `/comments`)
    - `dialog` — диалог

  - **`user_id`** — integer &lt;int64&gt; · Nullable

    ID получателя сообщения в диалоге (пользователя или бота). Если сообщение отправлено в групповой чат или канал, то параметр отсутствует

  - **`post_id`** — string · Nullable

    Идентификатор поста в канале, к которому оставлен комментарий

- **`timestamp`** — integer &lt;int64&gt;

  Время создания сообщения в формате Unix timestamp в миллисекундах

- **`link`** — object LinkedMessage · Nullable · optional

  Пересланное или ответное сообщение

  - **`type`** — enum MessageLinkType

    Возможные значения в enum: `"forward"` `"reply"`

    Тип связанного сообщения:
    - `"reply"` — ответ на сообщение в чате
    - `"forward"` — пересланное сообщение в чате

  - **`sender`** — object [User](../../../../objects/User.md) · optional

    Пользователь или бот, отправивший сообщение

  - **`chat_id`** — integer &lt;int64&gt; · optional

    Чат или канал, в котором сообщение было изначально опубликовано. Только для пересланных сообщений с `type = forward`. Как получить ID — в [разделе «Получение chat_id»](../../../../index.md#Получение%20chat_id)

  - **`message`** — object MessageBody

    Информация о сообщении

    - **`mid`** — string

      Уникальный ID сообщения

    - **`seq`** — integer &lt;int64&gt;

      ID расположения сообщения в чате по порядку

    - **`text`** — string · Nullable

      Текст сообщения

    - **`attachments`** — Attachment[] · Nullable

      Вложения сообщения. Могут быть одним из типов `attachment`, описанных в схеме ниже
      Если поле пустое или равно `null`, изменений не произойдет. Если массив пуст, все вложения будут удалены

      - Вариант `type = "image"` — PhotoAttachment

        Вложение изображения

        - **`payload`** — object PhotoAttachmentPayload

          Данные, использованные для отправки изображения

          - **`photo_id`** — integer &lt;int64&gt;

            Уникальный ID этого изображения

          - **`token`** — string

            Токен вложения — уникальный ID загруженного медиа: изображения, аудио, видео или файла. Возвращается в ответ на вызов [POST /uploads](../../../POST/uploads.md)

          - **`url`** — string

            URL изображения. Время жизни ссылки ограниченно. Срок истечения указан в параметре `expires` — если он истёк, ссылку необходимо запросить повторно. Доступно в веб-клиентe и на Android

      - Вариант `type = "video"` — VideoAttachment

        - **`payload`** — object MediaAttachmentPayload

          Результат загрузки аудио/видео

          - **`url`** — string

            URL вложения, который возвращается в объекте [Update](../../../../objects/Update.md) после отправки сообщения в чат или канал

            В случае с вложенным видео прямую ссылку также можно получить с помощью метода [`GET /videos/{-videoToken-}`](../../videos/-videoToken-.md)

          - **`token`** — string

            Используйте `token`, если вы пытаетесь повторно использовать одно и то же вложение в другом сообщении

        - **`thumbnail`** — string · Nullable · optional

          Миниатюра видео

          - **`url`** — string

            URL изображения

        - **`width`** — integer · Nullable · optional

          Ширина видео

        - **`height`** — integer · Nullable · optional

          Высота видео

        - **`duration`** — integer · Nullable · optional

          Длина видео в секундах

      - Вариант `type = "audio"` — AudioAttachment

        - **`payload`** — object MediaAttachmentPayload

          Результат загрузки аудио/видео

          - **`url`** — string

            URL вложения, который возвращается в объекте [Update](../../../../objects/Update.md) после отправки сообщения в чат или канал

            В случае с вложенным видео прямую ссылку также можно получить с помощью метода [`GET /videos/{-videoToken-}`](../../videos/-videoToken-.md)

          - **`token`** — string

            Используйте `token`, если вы пытаетесь повторно использовать одно и то же вложение в другом сообщении

        - **`transcription`** — string · Nullable · optional

          Аудио транскрипция

      - Вариант `type = "file"` — FileAttachment

        - **`payload`** — object FileAttachmentPayload

          Результат загрузки файла

          - **`url`** — string

            URL вложения, который возвращается в объекте [Update](../../../../objects/Update.md) после отправки сообщения в чат или канал

            В случае с вложенным видео прямую ссылку также можно получить с помощью метода [`GET /videos/{-videoToken-}`](../../videos/-videoToken-.md)

          - **`token`** — string

            Используйте `token`, если вы пытаетесь повторно использовать одно и то же вложение в другом сообщении.

        - **`filename`** — string

          Имя загруженного файла

        - **`size`** — integer &lt;int64&gt;

          Размер файла в байтах

      - Вариант `type = "sticker"` — StickerAttachment

        - **`payload`** — object StickerAttachmentPayload

          Результат отправки стикера

          - **`url`** — string

            URL вложения, который возвращается в объекте [Update](../../../../objects/Update.md) после отправки сообщения в чат или канал

            В случае с вложенным видео прямую ссылку также можно получить с помощью метода [`GET /videos/{-videoToken-}`](../../videos/-videoToken-.md)

          - **`code`** — string

            ID стикера

        - **`width`** — integer

          Ширина стикера

        - **`height`** — integer

          Высота стикера

      - Вариант `type = "contact"` — ContactAttachment

        - **`payload`** — object ContactAttachmentPayload

          Результат загрузки данных контакта в сообщение

          - **`vcf_info`** — string · Nullable · optional

            Информация о пользователе в формате VCF

          - **`hash`** — string · Nullable · optional

            Хеш информации о пользователе в формате VCF. Используется для проверки того, что пользователь поделился номером телефона, привязанным к его аккаунту в МАКС
            Подробнее — [в разделе «Типы кнопок»](../../../../index.md#Типы%20кнопок)

          - **`max_info`** — object [User](../../../../objects/User.md) · Nullable · optional

            Информация о пользователе

      - Вариант `type = "inline_keyboard"` — InlineKeyboardAttachment

        Кнопки в сообщении

        - **`payload`** — object Keyboard

          Клавиатура — это двумерный массив кнопок. Подробнее о типах кнопок и клавиатуре в ботах — [в разделе «Клавиатура для чат-бота»](../../../../index.md#Клавиатура%20для%20чат-бота)

          - **`buttons`** — Button[][]

            - Вариант `type = "callback"` — CallbackButton

              После нажатия на такую кнопку клиент отправляет на сервер полезную нагрузку, которая содержит

              - **`text`** — string

                от `1` до `128` символов

                Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

              - **`payload`** — string

                до `1024` символов

                Токен кнопки

            - Вариант `type = "link"` — LinkButton

              После нажатия на такую кнопку пользователь переходит по ссылке, которую она содержит

              - **`text`** — string

                от `1` до `128` символов

                Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

              - **`url`** — string

                до `2048` символов

            - Вариант `type = "request_geo_location"` — RequestGeoLocationButton

              После нажатия на такую кнопку клиент отправляет новое сообщение с вложением текущего географического положения пользователя

              - **`text`** — string

                от `1` до `128` символов

                Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

              - **`quick`** — boolean · optional

                По умолчанию: `false`

                Если `true`, отправляет местоположение без запроса подтверждения пользователя
                Если `false`, возвращает модальное окно с дополнительным уточнением об отправке местоположения

            - Вариант `type = "request_contact"` — RequestContactButton

              После нажатия на такую кнопку клиент отправляет новое сообщение с вложением текущего контакта пользователя

              - **`text`** — string

                от `1` до `128` символов

                Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

            - Вариант `type = "open_app"` — OpenAppButton

              Кнопка для запуска мини-приложения

              - **`text`** — string

                от `1` до `128` символов

                Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

              - **`web_app`** — string · optional

                Публичное имя (username) бота или ссылка на него, чьё мини-приложение надо запустить

              - **`contact_id`** — integer &lt;int64&gt; · optional

                Идентификатор бота, чьё мини-приложение надо запустить

              - **`payload`** — string · optional

                Параметр запуска, который будет передан в [initData](../../../../../docs/webapps/bridge.md#window.WebApp.initData) мини-приложения

            - Вариант `type = "message"` — MessageButton

              Кнопка для запуска мини-приложения

              - **`text`** — string

                от `1` до `128` символов

                Текст кнопки, который будет отправлен в чат от лица пользователя

            - Вариант `type = "clipboard"` — ClipboardButton

              При нажатии на кнопку с типом `clipboard` текст, указанный в свойстве `payload`, копируется в буфер обмена

              - **`text`** — string

                от `1` до `128` символов

                Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

              - **`payload`** — string

                до `1024` символов

                Текст, который будет скопирован

      - Вариант `type = "share"` — ShareAttachment

        - **`payload`** — object ShareAttachmentPayload

          Данные, использованные для прикрепления к сообщению предпросмотра контента по внешнему URL

          - **`url`** — string · Nullable · optional

            от `1` символа

            URL, прикреплённый к сообщению для предпросмотра медиавложения

          - **`token`** — string · Nullable · optional

            Токен вложения

        - **`title`** — string · Nullable · optional

          Заголовок предпросмотра ссылки.

        - **`description`** — string · Nullable · optional

          Описание предпросмотра ссылки

        - **`image_url`** — string · Nullable · optional

          Изображение предпросмотра ссылки

      - Вариант `type = "location"` — LocationAttachment

        - **`latitude`** — number &lt;double&gt;

          Широта, до 15 символов

        - **`longitude`** — number &lt;double&gt;

          Долгота, до 15 символов

    - **`markup`** — MarkupElement[] · Nullable · optional

      Разметка текста сообщения. Подробнее — в разделе [Форматирование](../../../../index.md#Форматирование%20текста%20в%20сообщениях)

      - Вариант `type = "strong"` — StrongMarkup

        Представляет **жирный** текст

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

      - Вариант `type = "emphasized"` — EmphasizedMarkup

        Представляет *курсив*

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

      - Вариант `type = "monospaced"` — MonospacedMarkup

        Представляет `моноширинный` или блок ```код``` в тексте

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

      - Вариант `type = "link"` — LinkMarkup

        Представляет ссылку в тексте

        **Обратите внимание**: в тексте комментариев гиперссылки не поддерживаются

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

        - **`url`** — string

          от `1` до `2048` символов

          URL ссылки

      - Вариант `type = "strikethrough"` — StrikethroughMarkup

        Представляет ~зачёкрнутый~ текст

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

      - Вариант `type = "underline"` — UnderlineMarkup

        Представляет <ins>подчёркнутый</ins> текст

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

      - Вариант `type = "user_mention"` — UserMentionMarkup

        Представляет упоминание пользователя в тексте. Упоминание может быть как по имени пользователя, так и по ID, если у пользователя нет имени

        **Обратите внимание**: в тексте комментариев упоминание пользователя не поддерживаются

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

        - **`user_link`** — string · Nullable · optional

          `@username` упомянутого пользователя

        - **`user_id`** — integer &lt;int64&gt; · Nullable · optional

          ID упомянутого пользователя без имени

      - Вариант `type = "heading"` — HeadingMarkup

        Представляет заголовок

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

      - Вариант `type = "highlighted"` — HighlightedMarkup

        Представляет выделенный текст

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

      - Вариант `type = "quote"` — QuoteMarkup

        Представляет цитату

        - **`from`** — integer &lt;int32&gt;

          Индекс начала элемента разметки в тексте. Нумерация с нуля

        - **`length`** — integer &lt;int32&gt;

          Длина элемента разметки в символах

- **`body`** — object MessageBody

  Содержимое сообщения. Текст + вложения. Может быть `null`, если сообщение содержит только пересланное сообщение

  - **`mid`** — string

    Уникальный ID сообщения

  - **`seq`** — integer &lt;int64&gt;

    ID расположения сообщения в чате по порядку

  - **`text`** — string · Nullable

    Текст сообщения

  - **`attachments`** — Attachment[] · Nullable

    Вложения сообщения. Могут быть одним из типов `attachment`, описанных в схеме ниже
    Если поле пустое или равно `null`, изменений не произойдет. Если массив пуст, все вложения будут удалены

    - Вариант `type = "image"` — PhotoAttachment

      Вложение изображения

      - **`payload`** — object PhotoAttachmentPayload

        Данные, использованные для отправки изображения

        - **`photo_id`** — integer &lt;int64&gt;

          Уникальный ID этого изображения

        - **`token`** — string

          Токен вложения — уникальный ID загруженного медиа: изображения, аудио, видео или файла. Возвращается в ответ на вызов [POST /uploads](../../../POST/uploads.md)

        - **`url`** — string

          URL изображения. Время жизни ссылки ограниченно. Срок истечения указан в параметре `expires` — если он истёк, ссылку необходимо запросить повторно. Доступно в веб-клиентe и на Android

    - Вариант `type = "video"` — VideoAttachment

      - **`payload`** — object MediaAttachmentPayload

        Результат загрузки аудио/видео

        - **`url`** — string

          URL вложения, который возвращается в объекте [Update](../../../../objects/Update.md) после отправки сообщения в чат или канал

          В случае с вложенным видео прямую ссылку также можно получить с помощью метода [`GET /videos/{-videoToken-}`](../../videos/-videoToken-.md)

        - **`token`** — string

          Используйте `token`, если вы пытаетесь повторно использовать одно и то же вложение в другом сообщении

      - **`thumbnail`** — string · Nullable · optional

        Миниатюра видео

        - **`url`** — string

          URL изображения

      - **`width`** — integer · Nullable · optional

        Ширина видео

      - **`height`** — integer · Nullable · optional

        Высота видео

      - **`duration`** — integer · Nullable · optional

        Длина видео в секундах

    - Вариант `type = "audio"` — AudioAttachment

      - **`payload`** — object MediaAttachmentPayload

        Результат загрузки аудио/видео

        - **`url`** — string

          URL вложения, который возвращается в объекте [Update](../../../../objects/Update.md) после отправки сообщения в чат или канал

          В случае с вложенным видео прямую ссылку также можно получить с помощью метода [`GET /videos/{-videoToken-}`](../../videos/-videoToken-.md)

        - **`token`** — string

          Используйте `token`, если вы пытаетесь повторно использовать одно и то же вложение в другом сообщении

      - **`transcription`** — string · Nullable · optional

        Аудио транскрипция

    - Вариант `type = "file"` — FileAttachment

      - **`payload`** — object FileAttachmentPayload

        Результат загрузки файла

        - **`url`** — string

          URL вложения, который возвращается в объекте [Update](../../../../objects/Update.md) после отправки сообщения в чат или канал

          В случае с вложенным видео прямую ссылку также можно получить с помощью метода [`GET /videos/{-videoToken-}`](../../videos/-videoToken-.md)

        - **`token`** — string

          Используйте `token`, если вы пытаетесь повторно использовать одно и то же вложение в другом сообщении.

      - **`filename`** — string

        Имя загруженного файла

      - **`size`** — integer &lt;int64&gt;

        Размер файла в байтах

    - Вариант `type = "sticker"` — StickerAttachment

      - **`payload`** — object StickerAttachmentPayload

        Результат отправки стикера

        - **`url`** — string

          URL вложения, который возвращается в объекте [Update](../../../../objects/Update.md) после отправки сообщения в чат или канал

          В случае с вложенным видео прямую ссылку также можно получить с помощью метода [`GET /videos/{-videoToken-}`](../../videos/-videoToken-.md)

        - **`code`** — string

          ID стикера

      - **`width`** — integer

        Ширина стикера

      - **`height`** — integer

        Высота стикера

    - Вариант `type = "contact"` — ContactAttachment

      - **`payload`** — object ContactAttachmentPayload

        Результат загрузки данных контакта в сообщение

        - **`vcf_info`** — string · Nullable · optional

          Информация о пользователе в формате VCF

        - **`hash`** — string · Nullable · optional

          Хеш информации о пользователе в формате VCF. Используется для проверки того, что пользователь поделился номером телефона, привязанным к его аккаунту в МАКС
          Подробнее — [в разделе «Типы кнопок»](../../../../index.md#Типы%20кнопок)

        - **`max_info`** — object [User](../../../../objects/User.md) · Nullable · optional

          Информация о пользователе

    - Вариант `type = "inline_keyboard"` — InlineKeyboardAttachment

      Кнопки в сообщении

      - **`payload`** — object Keyboard

        Клавиатура — это двумерный массив кнопок. Подробнее о типах кнопок и клавиатуре в ботах — [в разделе «Клавиатура для чат-бота»](../../../../index.md#Клавиатура%20для%20чат-бота)

        - **`buttons`** — Button[][]

          - Вариант `type = "callback"` — CallbackButton

            После нажатия на такую кнопку клиент отправляет на сервер полезную нагрузку, которая содержит

            - **`text`** — string

              от `1` до `128` символов

              Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

            - **`payload`** — string

              до `1024` символов

              Токен кнопки

          - Вариант `type = "link"` — LinkButton

            После нажатия на такую кнопку пользователь переходит по ссылке, которую она содержит

            - **`text`** — string

              от `1` до `128` символов

              Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

            - **`url`** — string

              до `2048` символов

          - Вариант `type = "request_geo_location"` — RequestGeoLocationButton

            После нажатия на такую кнопку клиент отправляет новое сообщение с вложением текущего географического положения пользователя

            - **`text`** — string

              от `1` до `128` символов

              Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

            - **`quick`** — boolean · optional

              По умолчанию: `false`

              Если `true`, отправляет местоположение без запроса подтверждения пользователя
              Если `false`, возвращает модальное окно с дополнительным уточнением об отправке местоположения

          - Вариант `type = "request_contact"` — RequestContactButton

            После нажатия на такую кнопку клиент отправляет новое сообщение с вложением текущего контакта пользователя

            - **`text`** — string

              от `1` до `128` символов

              Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

          - Вариант `type = "open_app"` — OpenAppButton

            Кнопка для запуска мини-приложения

            - **`text`** — string

              от `1` до `128` символов

              Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

            - **`web_app`** — string · optional

              Публичное имя (username) бота или ссылка на него, чьё мини-приложение надо запустить

            - **`contact_id`** — integer &lt;int64&gt; · optional

              Идентификатор бота, чьё мини-приложение надо запустить

            - **`payload`** — string · optional

              Параметр запуска, который будет передан в [initData](../../../../../docs/webapps/bridge.md#window.WebApp.initData) мини-приложения

          - Вариант `type = "message"` — MessageButton

            Кнопка для запуска мини-приложения

            - **`text`** — string

              от `1` до `128` символов

              Текст кнопки, который будет отправлен в чат от лица пользователя

          - Вариант `type = "clipboard"` — ClipboardButton

            При нажатии на кнопку с типом `clipboard` текст, указанный в свойстве `payload`, копируется в буфер обмена

            - **`text`** — string

              от `1` до `128` символов

              Видимый текст кнопки. Чтобы он отображался полностью, рекомендуем не превышать заданное количество символов в зависимости от размещения текста: `20` символов — при 1 кнопке в ряду, `10` — при 2, `5` — при 3, `3` — при 4

            - **`payload`** — string

              до `1024` символов

              Текст, который будет скопирован

    - Вариант `type = "share"` — ShareAttachment

      - **`payload`** — object ShareAttachmentPayload

        Данные, использованные для прикрепления к сообщению предпросмотра контента по внешнему URL

        - **`url`** — string · Nullable · optional

          от `1` символа

          URL, прикреплённый к сообщению для предпросмотра медиавложения

        - **`token`** — string · Nullable · optional

          Токен вложения

      - **`title`** — string · Nullable · optional

        Заголовок предпросмотра ссылки.

      - **`description`** — string · Nullable · optional

        Описание предпросмотра ссылки

      - **`image_url`** — string · Nullable · optional

        Изображение предпросмотра ссылки

    - Вариант `type = "location"` — LocationAttachment

      - **`latitude`** — number &lt;double&gt;

        Широта, до 15 символов

      - **`longitude`** — number &lt;double&gt;

        Долгота, до 15 символов

  - **`markup`** — MarkupElement[] · Nullable · optional

    Разметка текста сообщения. Подробнее — в разделе [Форматирование](../../../../index.md#Форматирование%20текста%20в%20сообщениях)

    - Вариант `type = "strong"` — StrongMarkup

      Представляет **жирный** текст

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

    - Вариант `type = "emphasized"` — EmphasizedMarkup

      Представляет *курсив*

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

    - Вариант `type = "monospaced"` — MonospacedMarkup

      Представляет `моноширинный` или блок ```код``` в тексте

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

    - Вариант `type = "link"` — LinkMarkup

      Представляет ссылку в тексте

      **Обратите внимание**: в тексте комментариев гиперссылки не поддерживаются

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

      - **`url`** — string

        от `1` до `2048` символов

        URL ссылки

    - Вариант `type = "strikethrough"` — StrikethroughMarkup

      Представляет ~зачёкрнутый~ текст

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

    - Вариант `type = "underline"` — UnderlineMarkup

      Представляет <ins>подчёркнутый</ins> текст

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

    - Вариант `type = "user_mention"` — UserMentionMarkup

      Представляет упоминание пользователя в тексте. Упоминание может быть как по имени пользователя, так и по ID, если у пользователя нет имени

      **Обратите внимание**: в тексте комментариев упоминание пользователя не поддерживаются

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

      - **`user_link`** — string · Nullable · optional

        `@username` упомянутого пользователя

      - **`user_id`** — integer &lt;int64&gt; · Nullable · optional

        ID упомянутого пользователя без имени

    - Вариант `type = "heading"` — HeadingMarkup

      Представляет заголовок

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

    - Вариант `type = "highlighted"` — HighlightedMarkup

      Представляет выделенный текст

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

    - Вариант `type = "quote"` — QuoteMarkup

      Представляет цитату

      - **`from`** — integer &lt;int32&gt;

        Индекс начала элемента разметки в тексте. Нумерация с нуля

      - **`length`** — integer &lt;int32&gt;

        Длина элемента разметки в символах

- **`stat`** — object MessageStat · Nullable · optional

  Статистика просмотров постов и репостов — возвращается только для каналов

  - **`views`** — integer

    Количество пользователей, которые увидели пост или репост в канале. Просмотр засчитывается, когда пост или репост попадает в область видимости экрана
    Если это репост, то будет показано количество именно его просмотров

- **`url`** — string · Nullable · optional

  Публичная ссылка на пост в канале. Отсутствует для диалогов и групповых чатов

## Коды ответов

| Код | Описание |
| --- | --- |
| `200` | Информация о сообщении с ID, указанным в запросе |
| `401` | Ошибка авторизации. Токен `access_token` указан некорректно или недействителен |
| `404` | В случае, если сообщение не найдено или бот не имеет доступа к нему |
| `500` | Внутренняя ошибка сервера |

---

Источник: <https://dev.max.ru/docs-api/methods/GET/messages/-messageId->
